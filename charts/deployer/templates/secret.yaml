apiVersion: v1
kind: Secret
metadata:
  name: deployer
  namespace: default
  annotations:
    kubernetes.io/service-account.name: default
data:
  token: {{ .Values.global.k8Token }}
type: kubernetes.io/service-account-token
